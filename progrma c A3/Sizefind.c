#include<stdio.h>
int main(){
    int inttype;
    float floattype;
    char chartype;
    printf("Size of int is:%u",sizeof(inttype));
    printf("\nSize of float is:%u",sizeof(floattype));
    printf("\nSize of char is:%u",sizeof(chartype));


}